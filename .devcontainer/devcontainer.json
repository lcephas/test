// Alpine base — used to exercise the pip-venv rung of the az install ladder.
{
	"name": "Alpine (apk → pip-venv az install)",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"features": {
		"ghcr.io/devcontainers/features/sshd:1": {}
	},
	"hostRequirements": {
		"memory": "8gb"
	}
}
